// ChatActions is no longer rendered as a standalone toolbar.
// The resource attachment button lives in MessageInput (the "+" button).
// Theme toggle and delete chat moved to the three-dot menu in the Chat header.
// This file is kept as an empty export so existing imports don't break.
const ChatActions = () => null;
export default ChatActions;
